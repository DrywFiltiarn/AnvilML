[package]
name = "anvilml-hardware"
version = "0.1.0"
edition = "2021"
description = "Hardware detection for AnvilML"

[features]
default = []
mock-hardware = []

[dependencies]
anvilml-core = { path = "../anvilml-core" }
serde_json = "1"
thiserror = "2"
sysinfo = "0.30"

[dev-dependencies]
serial_test = "3"
