[package]
name = "anvilml-hardware"
version.workspace = true
edition = "2021"

[dependencies]
anvilml-core = { path = "../anvilml-core" }
sysinfo = "0.32"

[dev-dependencies]
serial_test = "3.5"

[features]
mock-hardware = []
