[package]
name = "anvilml-hardware"
version = "0.1.0"
edition = "2021"
description = "Hardware detection for AnvilML"

[features]
default = []
mock-hardware = []

[dependencies]
anvilml-core = { path = "../anvilml-core" }
thiserror = "2"

[dev-dependencies]
serial_test = "3"
