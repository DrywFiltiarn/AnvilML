[package]
name = "anvilml-openapi"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[dependencies]
anvilml-core = { path = "../anvilml-core" }
anvilml-server = { path = "../anvilml-server" }
