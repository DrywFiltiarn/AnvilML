[package]
name = "anvilml-core"
version = "0.1.0"
edition = "2021"
description = "Core domain types and configuration for AnvilML"

[dependencies]
thiserror = "2"
serde_json = "1"
serde = { version = "1", features = ["derive"] }
toml = "0.8"
