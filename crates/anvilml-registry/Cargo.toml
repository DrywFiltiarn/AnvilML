[package]
name = "anvilml-registry"
version = "0.1.10"
edition.workspace = true
rust-version.workspace = true

[dependencies]
anvilml-core = { path = "../anvilml-core" }
chrono = { version = "0.4", features = ["serde"] }
digest = "0.11"
serde_json = "1.0"
sha2 = "0.11"
sqlx = { version = "0.9.0", features = ["sqlite", "runtime-tokio", "migrate", "chrono"] }
tokio = { version = "1.47.0", features = ["rt-multi-thread", "macros"] }
tracing = "0.1"
uuid = { version = "1.23", features = ["serde"] }

[dev-dependencies]
tempfile = "3.26"
uuid = { version = "1.23", features = ["v4"] }

[features]
# Gates insert_model_test() — test-only model row insertion for scheduler tests.
# Only enabled during `cargo test`, never in normal builds.
test-util = []
