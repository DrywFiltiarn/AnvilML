[package]
name = "backend"
version.workspace = true
edition = "2021"

[[bin]]
name = "anvilml"
path = "src/main.rs"

[dependencies]
anvilml-server = { path = "../crates/anvilml-server" }
tokio = { version = "1", features = ["full"] }

[features]
mock-hardware = ["anvilml-server/mock-hardware"]
