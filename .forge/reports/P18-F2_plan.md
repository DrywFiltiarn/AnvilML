# Plan Report: P18-F2

| Field       | Value                                             |
|-------------|---------------------------------------------------|
| Task ID     | P18-F2                                            |
| Phase       | 18 — HTTP/WebSocket Server Completion             |
| Description | CI: wire openapi-drift job to real generation + diff check |
| Depends on  | P18-F1                                            |
| Project     | anvilml                                           |
| Planned at  | 2026-07-12T17:50:00Z                              |
| Attempt     | 1                                                 |

## Objective

Replace the `openapi-drift` CI job in `.github/workflows/ci.yml` with the real Gate 2 check defined in `ENVIRONMENT.md §8`: run `cargo run -p anvilml-openapi` to regenerate `api/openapi.json`, then assert no uncommitted changes with `git diff --exit-code api/openapi.json`. The `api/openapi.json` file is already committed with real content (generated by P18-F1), so the gate will pass locally and on CI once wired.

## Scope

### In Scope
- Update `.github/workflows/ci.yml`: replace the placeholder `openapi-drift` job steps (current: single `echo "no openapi/config yet"`) with the real Gate 2 command sequence from `ENVIRONMENT.md §8`.
- Commit the existing `api/openapi.json` (already real, already tracked by git — no regeneration or content change needed).

### Out of Scope
None. `defers_to (from JSON): []` — empty. This task implements its full scope with no deferrals.

## Existing Codebase Assessment

**What already exists:** The `anvilml-openapi` binary (`crates/anvilml-openapi/src/main.rs`) is already fully implemented — it imports `anvilml_server::ApiDoc`, calls `ApiDoc::openapi().to_pretty_json()`, and writes the output to `api/openapi.json`. The `ApiDoc` struct (defined in `crates/anvilml-server/src/openapi.rs` as `pub struct ApiDoc` with `#[derive(utoipa::OpenApi)]`) is wired to all handler path annotations from P18-F1. The `api/openapi.json` file exists at 47,919 bytes, is committed to git, and is **not** in `.gitignore`. Running the binary twice produces identical output (confirmed idempotent). The current CI gate command `cargo run -p anvilml-openapi && git diff --exit-code api/openapi.json` exits 0 locally.

**Established patterns:** The CI workflow uses GitHub Actions with `actions/checkout@v6` and `actions-rust-lang/setup-rust-toolchain@v1` for Rust jobs. The existing `config-drift` job (also a placeholder echo) follows the same minimal structure. The `openapi-drift` job runs on `ubuntu-latest` and needs no Python worker venv — it only runs the Rust binary.

**Gap between design doc and current source:** None for this task. P18-F1 already made both the OpenAPI binary and `api/openapi.json` real. The only gap is the CI wiring — the job still says `echo "no openapi/config yet"`.

## Resolved Dependencies

None. This task modifies only a CI workflow YAML file and does not introduce or reference any external crate, package, or dependency version. The `anvilml-openapi` binary was already verified in P18-F1.

## Approach

1. **Read the current `openapi-drift` job** in `.github/workflows/ci.yml` (lines 116–122). It currently has two steps: checkout and a placeholder `echo`.

2. **Replace the placeholder steps** with the real Gate 2 sequence from `ENVIRONMENT.md §8`:
   - Keep the checkout step (already present).
   - Install the Rust toolchain (add the same `actions-rust-lang/setup-rust-toolchain@v1` step used by other CI jobs — required because `cargo run` needs a compiler).
   - Replace the `echo "no openapi/config yet"` step with:
     ```yaml
     - name: Generate OpenAPI spec
       run: cargo run -p anvilml-openapi
     - name: Check for drift
       run: git diff --exit-code api/openapi.json
     ```
   - The `cargo run -p anvilml-openapi` command regenerates the file in-place. If the committed spec is stale, the file changes and `git diff --exit-code` returns non-zero, causing the job to fail. If the spec is current, the file is unchanged and `git diff --exit-code` returns 0.

3. **Verify idempotency locally**: run `cargo run -p anvilml-openapi && git diff --exit-code api/openapi.json` to confirm it exits 0 (already confirmed: 47,919-byte file matches regenerated output).

4. **Stage changes**: `git add -A` to stage the CI workflow file. No other files change — `api/openapi.json` is already committed and up to date.

## Public API Surface

None. This task modifies only a CI workflow YAML file; no source code, types, or functions are added or changed.

## Files Affected

| Action | Path | Description |
|--------|------|-------------|
| Modify | `.github/workflows/ci.yml` | Replace `openapi-drift` job's placeholder echo steps with real Gate 2 commands (cargo run + git diff check) |

## Tests

This task modifies a CI workflow file, not source code. The acceptance criteria are shell-level commands that verify the gate works correctly.

| Test File | Test Name | What It Verifies | Acceptance Command |
|-----------|-----------|-----------------|-------------------|
| `.github/workflows/ci.yml` | openapi-drift job has real steps | The job contains `cargo run -p anvilml-openapi` and `git diff --exit-code api/openapi.json` (not echo) | `grep -c 'openapi-drift' .github/workflows/ci.yml` returns 1, and `grep 'echo.*no openapi' .github/workflows/ci.yml` returns 0 |
| Local gate | Gate 2 passes locally | The real gate command exits 0 against the committed spec | `cargo run -p anvilml-openapi && git diff --exit-code api/openapi.json` exits 0 |

## CI Impact

This task **is** a CI change. It modifies `.github/workflows/ci.yml` to replace the `openapi-drift` job's placeholder with real steps. No other CI jobs are affected. The change is additive-only: existing jobs (`rust-test`, `worker-test`, `config-drift`) are untouched. The `openapi-drift` job now requires the Rust toolchain (added as a step), which was previously unnecessary because it only ran `echo`.

## Platform Considerations

None identified. The CI workflow runs on `ubuntu-latest` and uses standard GitHub Actions Rust toolchain setup. The `anvilml-openapi` binary is cross-platform (pure Rust, no platform-specific code paths), so the gate works identically on any runner.

## Risks and Mitigations

| Risk | Likelihood | Impact | Mitigation |
|------|------------|--------|------------|
| `api/openapi.json` becomes stale between P18-F1 completion and this task's CI run (another handler change lands) | Low | Medium | The gate is designed to catch this: if drift exists, the CI job fails, forcing the developer to regenerate and commit. This is the intended behavior. |
| CI runner lacks Rust toolchain for `cargo run` step | Low | High | Add the `actions-rust-lang/setup-rust-toolchain@v1` step to the `openapi-drift` job (same pattern used by `rust-test`). Verified present in the existing CI file's `rust-test` job. |
| `git diff --exit-code` fails on CI because `api/openapi.json` has LF/CRLF differences from checkout | Low | Medium | `api/openapi.json` is a JSON text file already committed with LF line endings. The `.gitattributes` rule sets `*.rs = LF` but has no rule for `.json` — however, the file is generated by Rust on the same platform, so line endings are consistent. If needed, add `autocrlf = input` to the git config in the checkout step. |

## Acceptance Criteria

- [ ] `grep -c 'openapi-drift' .github/workflows/ci.yml` returns 1 (job exists)
- [ ] `grep 'echo.*no openapi' .github/workflows/ci.yml` returns exit code 1 (placeholder removed)
- [ ] `grep 'cargo run -p anvilml-openapi' .github/workflows/ci.yml` returns exit code 0 (real generation step present)
- [ ] `grep 'git diff --exit-code api/openapi.json' .github/workflows/ci.yml` returns exit code 0 (real drift check step present)
- [ ] `cargo run -p anvilml-openapi && git diff --exit-code api/openapi.json` exits 0 (gate passes locally)
