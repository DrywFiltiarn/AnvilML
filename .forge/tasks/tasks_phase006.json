[
  {
    "id": "P6-A1",
    "description": "anvilml-registry: ModelScanner directory walk and metadata derivation",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P5-B1"
    ],
    "context": "Create crates/anvilml-registry/src/scanner.rs: pub async fn scan(dirs:&[ModelDirConfig])->Vec<ModelMeta>. Infer ModelKind from path dir component (diffusion->Diffusion, text_encoders|clip->TextEncoder, vae->Vae, loras|lora->Lora, controlnet->ControlNet, upscale->Upscale, else Unknown). ModelDtype from filename (fp8->Fp8, fp16->Fp16, bf16->Bf16, fp32->Fp32, else Unknown). id=sha256_hex(first_1mib). tracing::debug!(path,reason) skipped files. tracing::info!(count,dir) on complete. cargo test -p anvilml-registry -- scanner exits 0 >=6 tests.",
    "tags": []
  },
  {
    "id": "P6-A2",
    "description": "anvilml-registry: ModelStore SQLite CRUD",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-A1"
    ],
    "context": "Create crates/anvilml-registry/src/store.rs: ModelStore{pool:SqlitePool}. Methods: upsert(meta:&ModelMeta)->Result<()>, get(id:&str)->Result<Option<ModelMeta>>, list(kind:Option<ModelKind>)->Result<Vec<ModelMeta>>, delete(id:&str)->Result<bool>. Use sqlx typed queries. Tests use open_in_memory(). cargo test -p anvilml-registry -- store exits 0 >=6 tests (upsert/get/list/delete/not-found).",
    "tags": []
  },
  {
    "id": "P6-A3",
    "description": "anvilml-registry: DeviceCapabilityStore backed by seed table",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-A2"
    ],
    "context": "Create crates/anvilml-registry/src/device_store.rs: DeviceCapabilityStore{pool:SqlitePool}. pub async fn get(&self,vendor_id:u16,device_id:u16)->Result<Option<DeviceRow>,AnvilError>. DeviceRow{vendor_id:u16,device_id:u16,name:String,arch:String,fp32:bool,fp16:bool,bf16:bool,fp8:bool,fp4:bool,flash_attention:bool}. Query device_capabilities table seeded by SeedLoader. Booleans stored as INTEGER 0/1; map via field!=0. cargo test -p anvilml-registry -- device_store exits 0.",
    "tags": []
  },
  {
    "id": "P6-B1",
    "description": "anvilml-server: GET /v1/models and GET /v1/models/:id handlers",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-A3"
    ],
    "context": "Create crates/anvilml-server/src/handlers/models.rs: list_models(State<AppState>, Query<{kind:Option<ModelKind>}>)->Json<Vec<ModelMeta>> calling registry.list(kind). get_model(State<AppState>, Path<String>)->Result<Json<ModelMeta>,AnvilError> calling registry.get(id) returning 404 on None. Add registry:Arc<ModelStore> to AppState. Mount GET /v1/models and GET /v1/models/:id in build_router. cargo test -p anvilml-server exits 0.",
    "tags": []
  },
  {
    "id": "P6-B2",
    "description": "anvilml-server: POST /v1/models/rescan + startup scan",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-B1"
    ],
    "context": "Add rescan_models handler (POST /v1/models/rescan): responds 202 immediately, spawns tokio task to scan cfg.model_dirs and upsert results into ModelStore. In main.rs: run initial scan at startup (same logic). tracing::info!(count,dir) per scan. Place test safetensors in models/diffusion/; curl /v1/models returns item with kind=diffusion and dtype=Fp8 if filename contains fp8.",
    "tags": []
  },
  {
    "id": "P6-C1",
    "description": "anvilml-core: add db_name field to GpuDevice",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-B2"
    ],
    "context": "Add pub db_name:Option<String> to GpuDevice in crates/anvilml-core/src/types/hardware.rs after name field. Field is None at all construction sites; only populated by SQLite enrichment in P6-C2. Update all GpuDevice struct literals across workspace (detect.rs, mock.rs, detector impls, test files) to add db_name:None. Update test_hardware_info_json_roundtrip to set db_name:None and assert roundtrip. Bump anvilml-core and anvilml-hardware patch versions. cargo test --workspace --features mock-hardware exits 0; GET /v1/system includes db_name:null on all GPU devices.",
    "tags": []
  },
  {
    "id": "P6-C2",
    "description": "anvilml-hardware: SQLite capability enrichment in detect_all_devices",
    "phase": "6",
    "project": "anvilml",
    "prereqs": [
      "P6-C1"
    ],
    "context": "Add anvilml-registry={path:../anvilml-registry} to anvilml-hardware/Cargo.toml; bump patch version. In detect.rs: rename _pool->pool, update #[instrument] skip. After step e add step e2: DeviceCapabilityStore::new(pool.clone()).await; for each non-CPU GpuDevice call store.get(pci_vendor_id,pci_device_id).await. Ok(Some(row)): overwrite arch,fp32,fp16,bf16,fp8,fp4,flash_attention,capabilities_source=DeviceTable,db_name=Some(row.name). Ok(None): tracing::warn!(vendor_id,device_id,name). Err: tracing::error! non-fatal. Never overwrite dev.name. Remove step h deferred stub. cargo test --workspace --features mock-hardware exits 0.",
    "tags": []
  }
]